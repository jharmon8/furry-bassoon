package file;

import resources.Player;

public class AccountLoader {
    /**
     * load the player from persistent storage
     * @param username
     * @return loaded player, or null if player doesn't exist
     */
    public static Player loadPlayer(String username) { // TODO
        return null;
    }
}
